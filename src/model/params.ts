export interface params {
  DialogTitle: string;
  IsShow: boolean;
  DialogFlag: number;
}

export interface code {
  key: string;
  content: string;
}

export interface codeHeader {
  key: string;
  language: string;
  content: string;
}
